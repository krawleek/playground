<p>
  <b>мнения</b>
  <%= User.find(comment.user_id).email %>
</p>
<p>
	<b>говорят</b>
	<%= comment.body %>
</p>
<% if user_signed_in? && current_user.id == comment.user_id  %>
<p>
  <%= link_to 'уже так не считаю', [comment.post, comment],
               :confirm => 'Вы точно этого хотите?',
               :method => :delete %>
</p>
<% end %>
